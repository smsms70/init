tool to plan code.

doc: docker-compose.dev.yaml is just to test.
doc: Dockerfile in main directory actually makes the stuff.

app in development, still not fully tested!
api keys not fully secured!

