services:
  backend:
    build: ./back-go/
    ports:
    - "8080:8080"
    environment:
      - ADMIN_PASSWORD=secret_password
      - ADMIN_USER=admin
    develop:
      watch:
        - action: sync
          path: ./back-go
          target: /app

  frontend:
    build: ./front-web/
    depends_on:
      - backend
    ports: 
    - "5173:5173"
    develop:
      watch:
        - action: sync
          path: ./front-web
          target: /usr/src/app
          ignore: node_modules/
