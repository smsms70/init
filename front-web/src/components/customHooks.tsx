// import { useContext } from "react";
// import type { contextType }  from "../pages/Dashboard/dashboardElement_types";
//
// export const useDataContext = ({ dataContext }: {dataContext: contextType | null}) => {
//   if (dataContext == null) {
//     throw new Error('useDataContext must be used inside a DataContextProvider');
//   }
//   const context = useContext(dataContext);
//   if (!context) {
//     throw new Error('useDataContext must be used inside a DataContextProvider');
//   }
//   return context;
// };
